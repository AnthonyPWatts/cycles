# Project State

Last updated: 2026-07-20

Cycles is a local, runnable pre-alpha development MVP. It proves the server-authoritative loop from galaxy generation through orders, tick resolution, factual history, Cycle completion, and successor generation. It is not yet an alpha release, production game service, or balanced multiplayer game.

## Capability Summary

| Area | Implemented now | Important limit |
| --- | --- | --- |
| Galaxy | A deterministic 8-sector territorial graph with 64 systems, 91 routes, 16 gateway systems, distributed gateway fan-out, home systems, resources, strategic/history fields, and a curated three-empire development opening with six neutral fleets. The immutable profile catalogue also contains the 2-sector, 10-system, 13-route Twin Reaches Training topology, which an allow-listed Player can provision privately from the Games home. | The canonical 64-system scale is browser- and SQL-verified. Twin Reaches provisioning and its complete four-resolution Core journey are SQL-verified against ordinary commands and authoritative outcomes. The model permits up to six empire participants, but larger matches remain unproved rather than an implied client contract. |
| Tick execution | CLI tick runner, batch-one scheduled Worker, explicit Game/Cycle resolution, persisted scheduling capability and next-due time, Game-then-Cycle transaction locks, accepted authenticated-development-player trigger, persisted `CommandOpen -> Closing -> Sealed -> Resolving -> Publishing -> CommandOpen` lifecycle, deterministic sealed turn ledger, duplicate-running-tick guards, configurable persisted-running diagnostics, explicit inspected abandonment, and recovery state. | Production health, graceful shutdown evidence, several-Cycle fairness/capacity, leadership and deployment monitoring remain tracked by #132 and later MG-12 work. |
| Influence and economy | Fleet-derived influence, home pressure, resource sharing, 100-point strategic-programme priorities, military ship construction, expansion projection, and one research unlock. | Development and Innovation are locked at zero until their accepted programme models receive bounded implementations; long-run resource sinks are incomplete. |
| Orders | Durable move, hold, attack, colonise, cancellation, and supersession lifecycle with one confirmed next-tick intention per fleet, SQL Cycle-lock command mutations, a deterministic visibility-respecting game-AI policy, neutral/implicit Holds, command-closure Colonise Population reservation, sealed source/timing audit fields, and phased resolution independent of submission timestamps. | The dashboard does not expose Hold, fleet creation, or fleet splitting. Ariadne uses a small attack, local-defence, colonisation, and value-seeking movement policy; neutral factions remain positional Hold obstacles. Deeper roles, player-facing diplomacy, combat forecasting, and unresolved multi-faction combat remain separate work. |
| Colonisation | Population-funded outposts that add supported local presence without binary ownership. | No capture, destruction, migration, infrastructure, or cross-Cycle inheritance. |
| Combat | Deterministic first-pass combat, battle facts, losses, events, and admiral outcomes. | Deliberately primitive and not balanced. |
| Diplomacy | Persisted Neutral, War, Non-Aggression Pact, and Alliance states; attacks record aggression and cancel breached treaties; active allied fleets automatically extend both empires' live system visibility. | No player-facing offers or declarations. Next-tick durable actions, bilateral consent, unilateral war/termination, the first-version Alliance friendly-fire guard, and factual relationship history are accepted but not implemented. Q019-Q022 still gate the complete player-facing lifecycle. |
| History | Chronicle scoring and template reports, per-tick metrics, final rankings, major-battle selection, system history signals, and successor-Cycle continuity. | The accepted product rule resets player and empire mechanics while allowing history-driven galaxy echoes. The current name, significance, and strategic echo is only the first continuity pass; burned-system effects, historical route naming, long-run accumulation, transition fiction, and asynchronous AI narrative are not implemented. [#156](https://github.com/AnthonyPWatts/cycles/issues/156) owns the current boundary and [#157](https://github.com/AnthonyPWatts/cycles/issues/157) owns the transition premise. |
| Identity and visibility | Persistent human or game-AI players; Player-only external sign-in; account-level session and dashboard admission; an authenticated account Games projection including the zero-membership state; explicit selected-Game gameplay routes with complete Player/Game/Cycle/enrolment/participant/empire authority; allow-listed private Training provisioning independent of sign-in; one active match participant per empire and player in a Cycle; a maximum of six empires; a fixed Tony/Will Development selector; non-Development OIDC/cookie authentication; case-sensitive issuer/subject invitation mapping without edge whitespace; focused, audited local admin-role changes; protected dashboard; own-and-allied active-fleet fog-of-war. | Authentication still does not enrol a Player or create gameplay state. Compatibility routes remain pinned to the legacy Game, public join does not exist, and Training creation is a feature-flagged pilot rather than general registration. The selector remains suitable only behind the trusted-playground access boundary. |
| Persistence | Mandatory SQL Server API/Worker runtime, 27 ordered migrations, transaction locks, focused SQL account/admin/Training/tutorial commands, a bounded per-Player Game catalogue projection, focused one-Cycle reads, commands and resolution, targeted writes, a legacy Game/configuration/enrolment foundation with enforced same-scope relationships, persisted `SchedulingMode`/`NextTickAt`, strict v7 JSON transfer, bounded legacy-file conversion, Azure SQL playground cutover, and proved point-in-time restore. | API and Worker have no online `IGameStateStore` dependency. The normal operator CLI still contains legacy whole-state and implicit selection paths. The only online second-Game creator is the bounded private Training/fresh-attempt command; standard Game creation and normal multi-Game CLI operations do not yet exist. |
| Client | Public landing page with a provenance-labelled 30-second development film at a stable Cloudflare media URL, plus a protected account Games home above the playable dashboard; a server-ranked attention ledger and grouped Game memberships; an allow-listed Start Training offer that provisions or resumes Twin Reaches; URL-authoritative Game/workspace selection with stale-response rejection; an authored empire/Cycle shell and a fixed 150-turn progress ribbon; a Command triage hub with a data-driven council agenda, frontier schematic, command stream, strategic watch, programme controls, commitment calendar, command-stage status, complete phase order, and player-scoped turn forecast; focused Galaxy, Fleets, and History workspaces with pre-submit Move duration/arrival projections, first-class in-transit journey status, and phase-ordered Events; an authored Galaxy/Sector atlas with live overlays; an admiral-led local Day One guide for Standard plus a server-derived four-resolution Core foundations drawer for Training; one antiforgery-aware JSON transport; and responsive browser breakpoints. | The Games home still lacks lobby/archive experiences, and the complete responsive/accessibility journey states remain MG-10 work. Training copy is conditional on authoritative evidence, but the five-novice comprehension/time gate is not yet available. Move, income, Colonise reservation, automatic Military spending, and next-window progression projections describe the current planning state and can change before resolution; queued construction deliveries are existing commitments. The 150-turn ribbon is a presentation boundary rather than an automatic Cycle-end rule. Desktop/laptop command use is the accepted priority; narrow screens retain the core loop without equal mobile optimisation. The film's concept dramatisations show intended scale and tone, not simulated gameplay output. |

## Implemented Tick-Resolution Contract

The intended authoritative lifecycle is `CommandOpen -> Closing -> Sealed -> Resolving -> Publishing -> CommandOpen`; a failed tick enters the existing recovery-required boundary instead of partially publishing a turn.

The processing sequence is a gameplay rule. It decides which resources a player can spend, which fleets occupy a system when battle begins, whether a colonising fleet remains eligible, and when progression starts to affect play. The [Simulation Reference](simulation-reference.md#authoritative-processing-order) owns the phase-by-phase contract and its player consequences.

1. The configured deadline closes human command submission. The Development or operator **Close command window and advance** action invokes the same current-game closure early; it is not a readiness signal.
2. The latest human intentions are snapshotted. Missing fleet commands normalise to Hold.
3. The game-AI planner deterministically attacks a weaker locally visible faction, holds against a local threat without a clear advantage, establishes the best affordable eligible outpost, or moves towards the highest-value reachable expansion system. It uses public system value and topology plus its own state, never hidden human intentions or remote fleet positions. Neutral fleets still generate deterministic Holds.
4. Before sealing, each empire's otherwise-eligible Colonise intentions reserve Population from its stockpile plus projected current-turn income. A budget that cannot fund the complete set rejects the whole set, and those fleets receive implicit Holds.
5. The complete validated human, AI, neutral, and implicit-Hold turn ledger is durably sealed.
6. Resolution proceeds in deterministic batches: resources; mandatory economy and due construction; new programme spending and construction starts; recalls, arrivals, movement, and Holds; combat; colonisation; derived control, visibility, availability, and defeat state; next-window progression; factual events and Chronicle selection.
7. The tick commits atomically through the existing recovery boundary, then publishes the next command window.

Within a batch, submission time confers no initiative. Stable fleet and order identifiers make processing reproducible without granting gameplay priority. All movement resolves before combat; same-faction attacks against the same opposing faction in one system form one battle from the shared post-movement state. Current income may fund current spending; due ships may defend but cannot receive previously sealed commands; construction commissioned in the tick gains no progress in that tick; and progression unlocked during resolution takes effect in the next command window. Colonise Population is reserved for the complete otherwise-eligible set at closure and consumed only after each successful outpost. A later eligibility failure leaves its reservation unspent and cannot revive an intention rejected when the set was oversubscribed. A fleet that moves away can escape an attack, while a fleet that arrives can take part in defence. The first version has no implicit route interception or pursuit. Combat involving more than two independently hostile factions remains governed by the unresolved multi-empire combat decision rather than an invented alliance-side rule.

## Implemented Rules

### Cycle And Tick

- A default Cycle lasts 90 days with a 60-minute tick cadence.
- The next simulation step is `CurrentTickNumber + 1`.
- Every tick closes and seals one complete ledger before outcomes begin. Each active fleet has one human, game-AI planner, neutral planner, or implicit-Hold intention, and every ledger row records its source, sealed tick, and sealed time.
- Player order, cancellation, replacement, and priority mutations acquire the same Cycle lock as tick execution. A command cannot enter the turn after the tick has begun sealing it.
- Resolution runs resources; due construction; new programme spending; recalls, arrivals, movement, and Holds; combat; colonisation; derived metrics; next-window progression; and one-transaction publication in that order.
- The Worker checks immediately on startup and polls every 30 seconds by default. Each poll reads at most one due Standard Game/Cycle ordered by persisted `NextTickAt` and stable Cycle ID, then acquires Game followed by Cycle locks and rechecks the exact due candidate. A competing Worker observes a stale candidate rather than advancing the same Cycle twice. Training and self-paced Cycles are excluded from scheduled discovery; explicit resolution uses the same authoritative tick boundary.
- Tick work uses a focused transactional working copy. Mutable entities are isolated; append-only facts are rolled back if processing fails.
- A failed tick records diagnostics, marks the Cycle `RecoveryRequired`, and blocks further ticks until an operator clears or retries it.
- The CLI exposes `diagnostics`, `recovery`, `recovery details`, inspected `recovery abandon`, `recovery clear`, and `recovery retry`.
- Persisted `Running` attempts are suspicious after a configurable threshold that defaults to five minutes. Diagnostics report attempt identity, age, context, and recent finished durations without mutating state.
- A confirmed suspicious attempt can be marked failed only through the explicit operator/reason command; it remains recovery-blocked until normal repair and clear/retry.

### Curated Development Opening

- A normal SQL CLI seed creates the fixed `development-match-v2` scenario. A freshly migrated empty store remains empty until deliberate CLI or Docker provisioning; Development API and Worker hosts do not seed it implicitly. Explicit non-canonical dimensions retain generic deterministic generation.
- The canonical map contains 8 named sectors of 8 systems and 11 inter-sector bridges. Each sector has its own connected 10-route composition and exactly two gateway systems. Local routes take one tick and inter-sector bridges take two.
- Highly connected gateways receive additional strategic value and an initial historical signal, making topological hubs tactically desirable and more likely to become notable places.
- Tony, Will, and Ariadne are assigned deterministically to three empires whose home systems are always in distinct sectors. Tony and Will are human development accounts; Ariadne is a game-AI account.
- Each empire begins with three fleets containing 30, 18, and 12 ships, 100 of each resource, and a 67/33 Military-to-Expansion allocation. One starting admiral commands the home guard.
- A neutral Free Captains faction owns six weak fleets: one 8-ship local pressure fleet near each empire and three remote fleets of 8–14 ships. Neutral factions have no empire economy, participant, diplomacy, or resource generation.
- Every empire receives a genuine move, colonise, and attack opportunity through an empire-scoped `OpeningBriefingIssued` fact. Objective identifiers are stable and faction-based, so the dashboard does not infer intent from display names.
- Opening outcomes are resolved by the normal simulation and are not scripted.

### Influence, Resources, And Growth

- Active ships create presence; in-transit and destroyed fleets do not.
- A founding empire has minimum home-system presence of 10.
- Each system divides Industry, Research, and Population output in proportion to effective presence.
- Resources are non-negative stockpiles, with last-generated and last-spent values recorded separately.
- Priority weights must total 100 and allocate strategic effort across Development, Innovation, Military, and Expansion rather than mapping one-to-one to the three resource stockpiles. While only two programmes are active, Military and Expansion share the full allocation.
- The persisted Industry and Research weights remain compatibility names for Development and Innovation. Both are locked at zero in domain validation, persisted state, imports, and the dashboard until their programmes are active.
- Military priority spends available industry on ships costing 25 industry each. Construction takes three ticks. Due ships join a home fleet that sealed Hold, or form a reinforcement fleet at home when every existing home fleet has another sealed command; they never inherit an order sealed before they existed.
- Expansion priority increases effective presence by its percentage.
- At 200 stockpiled research, Survey Projection unlocks once and adds a further 10% effective-presence bonus.
- A colonial outpost costs 100 population and adds five local presence while its empire has an active fleet in the system.

### Orders And Combat

- Movement follows linked systems. Legal Move destinations expose the direct route's duration plus projected next-tick dispatch and inclusive arrival (`dispatch + travel - 1`); pending Move responses repeat the current projection. Resolution revalidates the link, then longer routes record authoritative departure and arrival ticks and put fleets in transit. An outbound empire fleet can queue Recall to its last occupied system; Recall resolves before passive arrival and uses elapsed outward travel as its return duration.
- A fleet can have at most one pending intention for a given execution tick. Current player commands target the next tick.
- Submitting the same intention again is idempotent. Replacing it with a different intention requires the current pending order ID as explicit confirmation.
- A confirmed replacement stays in immutable history as `Superseded` and links to the new pending order.
- Pending orders can be cancelled by the owning empire before their execution tick.
- Attack submission requires an active opposing fleet in the attacker's current system. The dashboard disables and guards the action when its visible local fleet detail contains no valid target, while the API independently validates authoritative state.
- Processing revalidates state, so an order that was valid when submitted can still be rejected later.
- Missing human commands become deterministic implicit Holds. Game-AI fleets use the ordered attack, local-defence, colonisation, and value-seeking movement policy, with Hold as the fallback; neutral fleets always Hold. Generated planner orders are durable audit facts, while non-human Holds do not create repetitive player-facing events.
- Submission timestamps do not order outcomes. Recalls, existing arrivals, and all moves resolve before any attack; attacks resolve before colonisation.
- Multiple attacking fleets from the same faction against the same opposing faction in one system resolve as one battle, with fleet loss allocation ordered by stable fleet identifier.
- Attack orders engage hostile active fleets in the attacker's current system.
- Combat randomness derives from persisted Cycle, tick, system, and attacking-fleet identifiers.
- Battle records preserve participants, ships before battle, losses, outcome, and fact JSON.
- Seeded fleets have named admirals. Battle history changes reputation and status; destruction can kill an assigned admiral.

### Chronicle And Cycle History

- Chronicle importance currently considers losses, strategic value, historical significance, underdog outcomes, very large losses, and notable admirals.
- Chronicle battle prose is deterministic template output validated against required source facts and stored separately from factual summaries.
- Completed ticks store each empire's map-control percentage, rank, winner flag, effective presence, and active ships.
- `cycle end` ranks active empires by `MapControlPercent`, stores final standings, selects the top 10% of battles by losses with a minimum of one, and records system history signals.
- Rankings remain per empire regardless of diplomatic relationships: allies do not pool map control or become joint winners.
- Influence and resource shares remain per empire regardless of diplomatic relationships: allied fleets may coexist in a system, but their effective presence is not pooled.
- `cycle next` creates a successor only after the source Cycle completes and no other Cycle is active. It carries participating players into new empires and preserves selected famous-system names, significance, and strategic character without carrying fleets, resources, ownership, rank benefits, or other participant-specific power.
- A newly generated successor records its source Cycle as `PreviousCycleId` and receives the next materialised configuration in the same legacy Game. Migration backfill deliberately leaves historical predecessor links null rather than guessing lineage.

### API And Dashboard

- Ordinary player endpoints expose filtered state and accept intentions through explicit response DTOs.
- JSON responses use explicit camelCase property names and camelCase string enums. Numeric enum input is rejected. Handled errors return the correct HTTP status with stable `code`, safe `message`, optional structured `details`, and optional `traceId`.
- Raw domain entities remain internal and are not returned to the dashboard.
- Event, battle, and tick DTOs omit internal `FactJson`. The dashboard receives a purpose-built empire-scoped opening-briefing contract; internal flexible fact storage remains unchanged.
- Player mutations derive empire authority from the authenticated development session rather than caller-supplied empire IDs.
- Players can see the full map structure, but exact presence, local fleets, events, last-tick facts, and Chronicle entries are limited by active-fleet visibility. Development admins can inspect all state.
- In Development, every authenticated player receives a **Close command window and advance** capability that invokes the same authoritative store operation used by the Worker and CLI. A confirmation identifies the game-wide closure and reports aggregate current-Cycle counts without exposing another empire's order detail. This does not change the player's role, visibility, or empire authority.
- Ordinary production players cannot execute ticks; a trusted admin can still use the protected operational endpoint.
- Every game-state endpoint derives an authenticated local actor. Outside Development, anonymous `/app.html` requests start OIDC, authenticated identities must be explicitly admitted, and `/` plus `/health` remain public unless the trusted-playground perimeter override is configured.
- External sign-in creates or updates only an active Human Player account. It does not create Game enrolment, participation, empire, or admiral state. `/auth/session`, `/games`, and `/app.html` admission resolve that account without requiring a Game; only a selected gameplay workspace requires an existing readable seat.
- External identities map by case-sensitive issuer and subject with no leading or trailing whitespace. Provider email, display name, groups, roles, and invitation state do not grant local admin authority. Explicit configured bootstrap and focused authenticated grant/revoke operations append high-severity audit records; routine revocation cannot remove the final active Human admin.
- Initial dashboard load and normal refresh use one typed, player-scoped bootstrap response built from one authoritative store snapshot. It carries the session, active Cycle, empire, visible galaxy, owned fleets, selected-fleet detail, bounded orders, a complete latest visible Event tick plus bounded older Events, visible Chronicle, opening briefing, and `turnResolution` presentation contract while retaining the narrower endpoints for focused interactions.
- Selected-fleet detail includes legal Move destinations with route duration, projected dispatch, and projected arrival. Pending Move responses repeat the current journey projection, retain their activation tick when the route becomes unavailable, and never override the resolver's route revalidation.
- The `turnResolution` contract exposes the current stage and command-acceptance flag, all nine phases and their player consequences, aggregate current-game pending/ledger counts, and an empire-scoped forecast for expected income, Colonise reservation, automatic Military spending and delivery, existing scheduled deliveries, and next-window Survey Projection. Forecast calculations reuse the simulation's income, reservation-eligibility, and Military programme boundaries; they do not mutate state.
- Command disables order, cancellation, recall, and priority controls outside `CommandOpen`. Its forecast and calendar distinguish projected effects from construction whose Industry was committed in an earlier turn. Empty future turns say that they contain no player orders or scheduled effects rather than implying that automatic play does not exist.
- Player Event DTOs include optional resolution-phase identity and order. History defaults to phase grouping within each tick; command-window and operational facts remain explicitly unphased. The bootstrap keeps the latest visible tick complete, then fills the older remainder up to the normal 100-Event bound so one turn result is not cut in half.
- The dashboard uses persistent hash-addressable views inside an authored empire/Cycle shell: **Command** for quick-session triage through a council agenda, real frontier schematic, recent command stream, strategic watch, resource reserves, linked 100-point priority drafting, and a calendar that combines pending orders with journeys already under way; **Galaxy** for a blue-violet/gold authored atlas with one fixed galaxy composition and eight fixed sector compositions, system-and-sector search, Galaxy/Sector/Local ranges, strategic lenses, live gateway/route emphasis, selected-system inspection, and a maximised cartography mode; **Fleets** for a selected-fleet command workspace, in-transit route and arrival progress, recall and recall cancellation, command availability, and filterable resolved orders; and **History** for separate, filterable **Chronicle** and **Events** records. The shell's illustrated navigation is designed to expand, but it does not expose empty Strategy or Diplomacy tabs before player-facing behaviour exists. Command agenda, schematic, watch, stream, and calendar content is derived from existing visibility-filtered briefing, fleet, order, event, resource, priority, and galaxy responses rather than new authority or speculative mechanics. The route-free artwork is a stable spatial layer for territory atmosphere and fixed node positions; route topology, names, hit targets, selection, presence, lens metrics, accessibility and command handoff remain data-driven SVG overlays. The Galaxy view keeps compact home, selected-system, and strongest-visible-flashpoint focus controls without covering the authored chart with a duplicate overview navigator or recent-selection history. Its inspector links adjacent systems and hands local player fleets directly into their command context without expanding the empire-scoped API or visibility boundary. The Command view keeps saved priority positions visible while a new allocation is being drafted, locks Development and Innovation at zero, transfers points only between Military and Expansion, and persists changes only through an explicit save.
- Desktop and laptop browsers are the primary command surface. The responsive layout retains readable narrow-screen access to the core loop without promising equal mobile optimisation or a touch-first interaction model.
- Fleet selection is the command context for Move, Attack, and Colonise, so action forms only ask for the target information that action needs. Chronicle entries expose their source tick and label both date and importance before the factual summary and narrative report.
- The Standard Day One guide remains scoped per player and seeded Cycle instance. Training replaces it with the durable `Core foundations` journey: four lessons evaluate only owned orders, events, outposts and battles from the selected Game/Cycle; presentation acknowledgement cannot manufacture progress. The drawer survives return through server state, pauses distinctly from explicit skip, exposes player-safe authoritative resolution, reports recovery without rollback, and can atomically supersede the old world with an idempotent fresh attempt.
- The guide teaches resources, priorities, active-fleet visibility, map inspection, movement, colonisation, attack, the phase order, projected versus committed effects, game-wide command-window closure, Events versus Chronicle, and the operator-driven Cycle end/successor boundary through the current UI. Its result gate checks that the briefing's real Move, Attack, and Colonise orders each received a processed or rejected post-tick outcome instead of treating any tick advance as completion.
- The public site's 30-second film intercuts labelled current-build Command and Galaxy capture with labelled concept dramatisations of gateway transit, battle, and Cycle continuity. Its source frames, prompts, render command, and audio provenance are recorded beside the master rather than presented as gameplay evidence. External consumers use the stable `/media/cycles-promo.mp4` and `/media/cycles-promo-poster.jpg` contract without duration-based names or manual cache-busting queries.

### Persistence

- `IGameStateStore` remains a legacy Core/CLI compatibility and whole-state maintenance boundary. API gameplay and the Worker use focused application contracts and contain no `IGameStateStore` dependency.
- API, Worker, and gameplay/operator CLI commands use SQL Server exclusively. No executable file-backed game store or implicit store-selection factory remains.
- Operator CLI export/import uses a v7 versioned envelope, requires every persisted collection and explicit scheduling fields, rejects incompatible or partial input, validates Game/configuration/enrolment provenance and same-Cycle references alongside identifiers/tick recovery/embedded JSON, protects overwrite/replacement with explicit confirmations, and reloads SQL after import. v1-v6 and retired unversioned imports receive the required deterministic compatibility adaptations before validation; the bounded `state convert-runtime-file` bridge does not modify its source.
- Complete exports contain private state across all empires, identities, audit context, and hidden facts. They are sensitive operator/developer artefacts, not player save files or database backups.
- API and Worker require a Cycles SQL connection unconditionally and fail startup clearly when it is absent. The CLI likewise rejects file paths for gameplay and operator commands. JSON remains bounded to versioned transfer, validation, legacy conversion, inspection, fixtures, and migration evidence.
- SQL Server migrations are plain ordered scripts under `database/migrations` and are tracked in `dbo.SchemaMigrations`.
- Migrations `026_add_tutorial_runs` and `027_add_tutorial_skips` are the latest schema migrations. They add versioned Game/Player/Cycle-scoped `TutorialRuns`, presentation acknowledgements, account-level first completion and explicit skip ledgers, one current run per Player/definition, request idempotency and restartable backfill for existing Twin Reaches Games. The run schema's two composite relationships bring the current exact same-Game/Cycle set to 52 enabled, trusted foreign keys; migration `023_enforce_cycle_scope_integrity` retains the original 50 and remains restartable in the expanded schema. Migration `025_add_cycle_scheduling` retains immutable scheduling capability and the bounded due index.
- The 022 foundation backfill remains additive, restartable, and fail-closed. Migration 023 then backfills `MatchParticipants.GameID`, requires the matching Game enrolment, and rejects contradictory or cross-scope rows before tightening the contract. Both migrations recognise the canonical map and Development scenario only from consistent facts plus persisted topology, otherwise retaining `legacy-unclassified`; neither invents predecessor links or silently repairs contradictory state.
- Historical battle membership is normalised in `BattleFleetParticipants` while the two legacy list columns remain compatibility projections. Migration accepts exact CSV or JSON string-array representations containing canonical D-format GUIDs, derives normalised rows only when none exist, and rejects malformed, duplicate, cross-Cycle, overlapping, or partial membership. Runtime reads apply the same exact-set rule, and focused tick writes persist membership only for their Cycle.
- `Cycles.Application` defines redacted Player accounts, Player-only sign-in and login commands, bounded trusted-player selection, audited admin-role commands, bounded keyset-paged per-player Game catalogues, neutral Game-access snapshots, typed private Training provisioning, the pure Core-foundations evidence evaluator, durable tutorial attempt commands, validated Game/Cycle scope, a complete `GameCommandContext`, a coherent one-Cycle view, and typed query/command results. Its SQL implementation uses direct projections and guarded writes rather than `LoadOrCreate`.
- A resolved command context binds the exact Player, Game enrolment, Game, Cycle, participant, empire, and Game-level permissions. Defeated and completed participation remains readable; withdrawn enrolment is unavailable; commands require every live authority row to remain active. Both read and command paths re-prove asserted organiser and administrator permissions, so a stale elevated context fails closed.
- `ICycleViewQuery` reads the selected Game and complete target-Cycle workspace in one serialisable transaction, excludes every other Cycle and Game, and redacts email, password hash, issuer, and subject from every loaded Player. Current gameplay endpoints are available under `/games/{gameId}`; compatibility routes authenticate first, resolve the deterministic legacy Game, and invoke the same scoped handlers. `ILegacyRuntimeScopeQuery` fails closed unless that Game has exactly one Active or RecoveryRequired Cycle.
- Scoped commands hold the existing Cycle lock and serialisable authority reads, allow only Fleet-order, strategic-priority, append-only Event, and Fleet-faction compatibility changes, guard new identifiers against omitted history, and persist only changed target-Cycle rows. Missing, mismatched, stale, or inactive authority is neutral, lock contention is typed busy, and callback, validation, hostile-ID, or persistence failure rolls the transaction back.
- Generic SQL saves fill a missing deterministic legacy foundation and otherwise preserve complete v7 Game/enrolment, scheduling and battle-membership fields while round-tripping row versions. Operational state import still rejects an additional or non-legacy Game before opening SQL. The focused Training writer instead inserts one newly materialised Game without loading, deleting, or rewriting another Game; the Worker and normal CLI remain explicitly selected or legacy-pinned.
- Generic SQL `Replace` and `Update` operations remain for explicit operator/CLI compatibility and synchronise the mapped prototype state under the broad `Cycles.GameState` lock. They are absent from API and Worker interfaces. Focused Player or admin-audit mutations retain global-then-narrow lock ordering while those operator paths coexist, preventing a stale whole-state snapshot from erasing a focused commit.
- Player commands acquire `Cycles.Tick.{CycleID}`. Resolution acquires `Cycles.Game.{GameID}` and then the Cycle lock, reloads one Cycle-scoped workspace, and persists targeted ledger/outcome and schedule rows without loading or rewriting unrelated history. Tutorial status/acknowledgement uses the same Game/Cycle order after the Player/definition attempt lock. Fresh attempt additionally vacates the one-current slot, materialises the replacement and supersedes the old Game/run in one transaction. Success advances `NextTickAt`; failure enters recovery and clears it.
- SQL Server-specific locking and persistence details remain contained in `Cycles.Infrastructure.SqlServer`; `Cycles.Core` and `IGameStateStore` remain independent of database packages.

### Trusted Hosted Playground

- `Cycles.Api` targets .NET 10 LTS and is deployed to an Azure App Service F1 Free plan for invited Development play-testing.
- GitHub Actions publishes a successful `main` build through workload identity federation; no long-lived Azure credential is stored in the repository or GitHub environment.
- The hosted process reads and writes Azure SQL database `CyclesDb` in France Central through the sole App Service connection string, `Cycles`. No state-path or SQL-activation setting remains. The final stopped JSON file is retained only as sensitive cutover evidence and is not a rollback or recovery path.
- The 15 July 2026 sector-schema deployment applied migration 015 and upgraded the preserved tick-3 opening in place. Manual deployment run `29446689039` later used the explicit destructive `reseed` input to supersede that intermediate crown with the compact territorial graph. Independent protected API and browser checks confirmed 8 sectors, 64 systems, 93 routes, 13 bridges, 16 gateways, sector size exactly 8, gateway fan-out up to three, the three-range toolbar, and a clean console. The deployment retries the initial migration connection while serverless Azure SQL wakes from auto-pause and always attempts to restart the app after maintenance.
- The database uses the Azure SQL free serverless offer with a 2-vCore maximum, 0.5-vCore minimum, 32 GB maximum, provider-default 60-minute auto-pause, local backup storage, seven-day point-in-time retention, and automatic pause rather than billing when the free allowance is exhausted.
- No `Cycles.Worker` process is deployed. Invited players use the accepted Development-only **Close command window and advance** capability.
- The hosting scope is protected by a read-only App Service plan lock and an Azure Policy deny list for unapproved platform resources. The policy deliberately permits the approved Azure SQL server/database; F1 quotas and SQL free-limit exhaustion behaviour are the enforced spend boundaries, while budget notifications are not treated as a hard cap.
- `cycles.anthonypwatts.co.uk` is routed through a Cloudflare Worker on the Free plan. Its static-assets binding serves the public landing shell and all image/video artwork without Azure egress; dashboard HTML/JavaScript/CSS, authentication, game APIs, and `/health` retain the Azure proxy path. The API publish package excludes `wwwroot/assets` and `wwwroot/media`, so the Azure website upload contains no video assets, and direct-origin media requests redirect to the custom domain.
- Dashboard navigation, resource, galaxy-overview, and sector artwork is delivered through 17 same-dimension q90 WebP derivatives while the authored PNGs remain as source masters. The derivatives reduce that runtime artwork set from 39.26 MiB to 4.08 MiB. In the measured 1600×900 initial state, the nine requested authored images fall from 15.38 MiB to 1.42 MiB without adding requests; opening Umbral Marches fetches one 313 KiB sector image.
- The public 1080p/30 fps film is an 11.54 MiB CRF 22 web derivative that satisfies Cloudflare's 25 MiB static-asset ceiling. Its canonical URL is duration-independent, while the former duration-based URL redirects permanently for existing links. The reproducible 34.26 MiB CRF 16 master is retained outside `wwwroot` under `tools/promo` and is not deployed to Azure or Cloudflare.
- The shared code admits Anthony and Will without adding a payment method or enabling usage overages. Inside that perimeter, the explicitly enabled trusted selector offers only the seeded Tony and Will human participants; Ariadne remains game-AI controlled. This is a trusted-playground boundary, not production identity, registration, or matchmaking.
- The whole-site access-code gate remains available as an explicit deployment override but is not active on the trusted playground. The accepted private-alpha and Production route contract keeps `/` and `/health` public while requiring external authentication and invited-player admission for `/app.html`.
- The managed-SQL cutover preserved all 23 persisted collection counts and 166 records, and the reopened health plus authenticated gameplay smoke passed. Azure SQL retains seven days of point-in-time recovery; an isolated restore at the post-cutover checkpoint reproduced the current schema, all collection counts, active tick 3, and zero unresolved recovery, then the temporary paid restore database was deleted.

## Verification

The 20 July MG-09 Core-journey slice built the complete solution with **0 warnings and 0 errors**, passed **490/490 non-SQL tests**, and passed the mandatory live SQL gate with **176/176 tests** and non-zero TRX evidence. Its focused **6-test** Training suite executes all four lessons through ordinary command submission and authoritative ticks, proves queued lesson readiness again inside the Game/Cycle resolution lock, accepts a legitimate battle outcome rather than promising victory, and persists completion across a new store. It also covers duplicate acknowledgement, pause/resume, explicit skip, recovery presentation, foreign Game/Cycle rejection, restartable 026/027 migration and backfill, atomic resolution/fresh-attempt races, idempotent replacement replay, and legacy-Game topology maintenance while a Training Game is simultaneously active. The canonical local database reports all **27 migrations** applied with none pending. JavaScript syntax and diff-whitespace checks passed. Complete responsive/keyboard/screen-reader journey evidence remains MG-10 work, and the five-novice completion/time gate remains Increment 2 rollout evidence.

The 20 July MG-08 Training-provisioning slice built the complete solution with **0 warnings and 0 errors**, passed **484/484 non-SQL tests**, and passed the mandatory live SQL gate with **172/172 tests** and non-zero TRX evidence. Its focused SQL cases prove that two concurrent requests with different request identifiers converge on one private Twin Reaches Game/Cycle, retain exactly one enrolment/participant, materialise the exact two-sector/ten-system/thirteen-route profile, and remain self-paced with no Worker due time. A normal Home Guard Move submitted through `ICycleCommandStore` then resolves through `ICycleResolutionStore` and is observed at Firstlight on T1 through a newly constructed store and command context. Feature-option tests cover allow-list exposure, dependency validation and invalid identifiers; dashboard/API contracts cover the offer, antiforgery-protected provisioning route, request identifier, account reload and ordinary selected-Game navigation. JavaScript syntax and diff-whitespace checks passed. The checked-in default keeps Training creation off; the trusted-playground deployment enables it only for Tony. MG-09 still owns the four-resolution server-derived journey, player-facing resolve policy, recovery and pause/skip/fresh-attempt semantics.

The 20 July MG-07 account Games-home slice built the complete solution with **0 warnings and 0 errors**, passed **481/481 non-SQL tests**, and passed the mandatory live SQL gate with **170/170 tests** and non-zero TRX evidence. Projection tests cover deterministic attention ranking, grouping, contextual actions and the zero-membership state; dashboard contracts cover account-first boot, exactly four gameplay workspaces, URL-authoritative selection and stale-generation rejection; the SQL catalogue evidence now includes authoritative start and scheduling fields. Live-browser checks covered account and selected-game navigation, independent Game/workspace URLs in two tabs, and desktop, tablet and mobile layouts. That pass found and corrected the Day One guide leaking over the account shell, then verified the account route without the overlay or horizontal scrolling. This completes MG-07 as a safe selection and presentation boundary; it does not create a second Game, provision Training, or implement the tutorial journey.

The 20 July MG-06 profile/factory slice built the complete solution with **0 warnings and 0 errors**, passed **474/474 non-SQL tests**, and passed the mandatory live SQL gate with **170/170 tests** and non-zero TRX evidence. The code-owned catalogue validates declared content hashes, key/version uniqueness, roster bounds, coordinate/reference integrity, legal travel times and connected topology at API, Worker and CLI startup. Twin Reaches proves its exact two-sector, ten-system, thirteen-route shape, its sole two-tick Gatehouse–Threshold bridge, one existing Human participant, account-free neutral actors, deterministic retry and ordinary first Move through `OrderService` and `TickEngine`. Both Standard and Training materialisations round-trip through the existing SQL contract with verified provenance and row versions. This completes MG-06 as a Core creation boundary; it does not create a Player-facing Training Game or second durable Game.

The 20 July MG-03–05 prerequisite cutover built the complete solution with **0 warnings and 0 errors**, passed **464/464 non-SQL tests**, and passed the mandatory live SQL gate with **168/168 tests** and non-zero TRX evidence. The SQL suite covers deterministic due ordering and exclusions, stale and duplicate Worker suppression, explicit self-paced resolution, recovery schedule clearing, hostile Game/Cycle tuples, authority revocation during resolution, lifecycle completion and reactivation including prior-Cycle and zero-participant enrolments, migration upgrade and direct-rerun idempotency, scheduling/configuration coherence triggers, seed idempotency, scope constraints, and the existing focused-store regressions. The canonical local database reports all **25 migrations** applied with `025_add_cycle_scheduling` latest. Selected-Game API and dashboard contracts cover non-disclosing reads and commands, dashboard bootstrap, antiforgery policy, client request generations, the empty online whole-state allowance, transfer v7, scheduling, completion, and recovery; PowerShell syntax and diff-whitespace checks passed. The SQL-backed Development gameplay smoke then passed token issue and post-login rotation, selected-Game reads, antiforgery-protected priority and Move commands, explicit manual resolution, processed movement, resources, and Events. It ran against a freshly migrated disposable database, which was removed after the pass, so the canonical local game data was not replaced or reseeded. This signs off the integrated MG-01–05 technical gate. It does not claim a Games home, second-Game factory/runtime, Twin Reaches, Training provisioning, tutorial journey, deployed several-Game Worker, or browser accessibility evidence.

The 19 July Player-account and admin cutover built the complete solution with no warnings, passed **433 non-SQL tests**, and passed the mandatory SQL gate with **153/153 tests** and non-zero TRX evidence. Its 20 added SQL facts cover Player-only external sign-in, monotonic login, redacted results, exact and bounded trusted selection, bootstrap/audit atomicity, global and narrow lock contention, guarded admin grant/revoke, final-active-Human-admin concurrency, identity-collation migration preservation, dirty-data rollback/retry, direct rerun, and migrator idempotency. Authentication, account sessions, dashboard admission, trusted login, and admin roles now use focused account boundaries; the frozen genuine whole-state call allowance fell from 27 to 21. Gameplay routes, Worker selection, resource-scoped routing, antiforgery, the Games home, and the second-Game gate remain unfinished.

The 19 July second MG-03 boundary slice built the complete solution with no warnings, passed **399 non-SQL tests**, and passed the mandatory live SQL gate with **133/133 tests** and non-zero TRX evidence. The focused store suite now has **16 SQL facts**. Its seven added facts prove exact Player/Game/Cycle/enrolment/participant/empire context resolution, a complete target-Cycle view with no foreign rows or Player credentials, defeated/completed read-only access, command-time revalidation of every live authority row and stale organiser/administrator permission, neutral rejection of hostile mixed tuples, and typed contention under the real Cycle lock. An independent authority and concurrency review found no remaining blocker. These contracts are still migration targets: the API and Worker have not yet cut over, and the second-Game hard gate remains closed.

The 19 July first MG-03 store-boundary slice built the complete solution with no warnings, passed **399 non-SQL tests**, and passed the mandatory live SQL gate with **126/126 tests** and non-zero TRX evidence. The nine added SQL facts cover redacted account projection, bounded stable per-player Game pagination, neutral inaccessible Game lookup, exact Game/Cycle mismatch, two-Game command isolation, foreign identifiers, omitted-history identifier collision, deletion and disallowed mutation rejection, callback failure, and full transaction rollback. An independent concurrency/security review found no blocker. These tests prove the new store boundary; they do not claim online consumer migration or MG-03 completion.

The 19 July MG-02 scope-integrity prerequisite passed **399 non-SQL tests** and the mandatory live SQL gate passed **117 SQL tests**, with non-zero execution proved from the required TRX evidence path. The SQL total includes a focused **65-test MG-02 set**: eight restartable and fail-closed synthetic 022-to-023 migration cases, one exact ordered/trusted/enabled metadata audit, 50 individual cross-scope foreign-key violations that assert the intended constraint, and six store cases covering normalised membership, partial-or-missing membership rejection, deletion order, supersession cleanup, and focused-tick isolation. The full store regression also proves dependency-safe two-phase persistence for replacement-order chains. Transfer, combat, clone, rollback, provisioning, seeding, and continuity tests cover the corresponding v6 in-memory contract. The local development database now reports all 24 migrations applied with none pending, and the generated Docker seed participates in the SQL parity checks. The isolated migration fixtures are synthetic upgrade evidence, not a production-backup restore rehearsal.

The 19 July additive Game-foundation prerequisite passed **394 non-SQL tests** and the mandatory live SQL gate passed **52 SQL tests**, with the latter reported from the required TRX evidence path. Coverage includes v4-to-v5 and legacy-runtime transfer adaptation, strict v5 lifecycle/configuration/provenance/lineage validation, the operational single-legacy-Game identity guard without metadata rederivation, immutable planned schedules alongside actual completion times, deterministic lifecycle-transition audit, deterministic and restartable 022 backfill, rollback on contradictory or partially expanded data, canonical-versus-unclassified profile provenance, hash/bounds/snapshot/audit constraints, per-Game operational uniqueness, generated Docker seed parity, row-version round trips, explicit two-Game tick isolation, predecessor-first persistence, and atomic successor creation. This is storage and compatibility groundwork only: no second-Game creation route, game picker, Training profile, or multi-game Worker selection is implemented.

The 19 July projected Move-journey slice passed **386 tests** with the normal repository helper, including a final **61-test focused pass** across the shared timing helper, API boundary, presentation contract, and dashboard contract. JavaScript syntax and diff-whitespace checks also passed. SQL integration tests were not enabled because the slice changes no persistence mapping. A freshly migrated and seeded disposable SQL database backed a real-browser playthrough at 1280×720 and 390×844: one-tick alternatives projected and resolved in the next turn, while Umbral Bastion to Red Lattice remained a two-tick journey from its pre-submit T3 dispatch/T4 arrival projection through the queued intention, turn calendar, committed transit, and authoritative T4 arrival. The narrow form remained readable without page-level horizontal overflow, and the browser reported no warnings or errors.

The 19 July player-facing turn-processing slice passed **369 tests** with the normal repository helper. A final **47-test focused pass** covered the turn-presentation contract, complete-latest-tick bootstrap boundary, and dashboard copy/control contracts; JavaScript syntax and diff whitespace checks also passed. SQL integration tests were not enabled because the slice changes no schema or persistence mapping. A fresh migrated and seeded disposable SQL database instead backed the real-browser journey: at 1440×900 and a 390×844 viewport, the nine phases, five forecast cards, commitment calendar, and game-wide closure dialog remained readable without page-level horizontal overflow. A real T0-to-T1 closure sealed 15 fleet intentions and published 12 Events and one battle; History grouped Will's four visible facts as phases 1, 3, and 8 followed by the unphased operational fact. The browser reported no warnings or errors.

Latest local verification on 2026-07-18 used the normal repository test helper with `CYCLES_SQL_INTEGRATION_CONNECTION_STRING` pointed at the migrated disposable local SQL Server:

```powershell
.\eng\test.ps1
```

Result: **294 tests passed, 0 failed**, including the opt-in SQL Server migration, state-store, Cycle-lock, and focused tick integration paths after migration `020_add_fleet_departure_tick` was applied to the local `CyclesDb`. The focused coverage proves deterministic ledger identities and command sources, recall-before-arrival timing, elapsed-travel return duration, recall cancellation through the existing pending-order boundary, movement-before-combat regardless of submission time, grouped same-faction attacks, construction reinforcement isolation, next-window progression, rollback, API contracts, JSON compatibility, SQL persistence, and coherent transit/recall presentation across navigation, Command, the commitment calendar, and Fleets. The Cloudflare Worker retains a further **5 passing Node routing tests**; Wrangler's deployment dry run accepted the complete static-asset set; and a Release publish produced no files under `wwwroot/assets` or `wwwroot/media`. The end-to-end Development gameplay smoke previously passed login, priorities, pending movement, turn advancement, processed movement, resources, events, and the 8-sector/64-system topology assertions.

The later WebP delivery slice passed **295 tests** with the normal helper and **49 focused dashboard, atlas, access-boundary, and edge-routing tests**. SQL integration was not repeated for this static-asset-only change. A real local authenticated browser load requested the WebP set, retained the 23-request page shape, transferred 1.42 MiB for the nine authored initial images, and rendered the Command, Galaxy, and selected-sector views cleanly at 1600×900.

The dashboard-bootstrap slice passed **302 tests** with the normal helper and **33 focused dashboard tests**. SQL integration was not repeated because the persistence implementation did not change. Across five repeatable authenticated local runs, an in-UI refresh fell from nine requests, nine store loads, 243 SQL commands, and a 1,450 ms median to one request, one load, 27 commands, and a 43 ms median. Warm browser reload data readiness fell from ten API requests and about 2,031 ms to one request and about 50 ms. The decoded response changed only from 61,610 to 62,113 bytes, while measured transfer including protocol overhead fell from 64,310 to 19,012 bytes. Tony and Will each received only their own fleets; an attempted foreign selected-fleet identifier fell back to an owned fleet, an owned selection survived refresh, and the opening briefing remained present.

The deterministic game-AI slice passed **316 tests** with the normal helper and the opt-in SQL Server integration path enabled, plus **12 focused game-AI tests**. Coverage proves favourable local attacks, local-threat Holds, treaty restraint, affordable high-value colonisation, value-seeking movement, stable objective and route ties, fallback Holds, remote-fleet and hidden-command isolation, positional neutral Holds, SQL ledger persistence, and an unattended eight-tick curated match that changes the map through movement, outposts, and combat. The end-to-end Development gameplay smoke also passed against a migrated disposable SQL database using an isolated Release build, covering login, priorities, movement submission, authoritative turn advancement, processed movement, resources, and Events.

The opening-agenda destination correction passed **339 tests** with the normal helper, including focused coverage for Tony's Umbral Bastion objective, Will's Far Meridian objective, and unavailable-target handling. A fresh migrated SQL-backed browser smoke confirmed that each agenda action opened the authoritative Home Guard, Move action, and destination without queueing an order; the browser reported no warnings or errors. SQL integration tests were not repeated because the persistence contract did not change.

Cloudflare revision `68d98bec-9017-47aa-b835-84b46484c699` deployed the three dashboard toolbar SVGs that were already referenced by the protected Azure-hosted stylesheet. All three live URLs return `200 image/svg+xml` with their expected view boxes, and an authenticated custom-domain browser load resolved the Guide, Advance turn, and Refresh masks at 20×20 pixels. The Azure deployment workflow now checks every SVG under `wwwroot/assets/icons` before stopping or publishing the API, so a dashboard revision cannot go live ahead of its required edge assets.

The retained film master and public web derivative both decode all 900 frames at 1920×1080 and 30 fps with 48 kHz stereo audio, and their final titles remain visible. The master keeps its documented −43.2 dB final-tail exception; the delivery-specific fade brings the web derivative inside the strict −45 dB final-250-ms gate.

The canonical territorial contract is covered at 8 sectors, 64 assigned systems, 80 locally varied routes, 11 inter-sector bridges, exactly two gateway systems per sector, distributed gateway fan-out, connected local/sector graphs, and an active Cycle ending 90 days after startup. The route-free authored atlas contract additionally verifies one galaxy image and eight sector images at native 992-pixel height and 1585–1586-pixel width. Live browser verification exercises Galaxy, Sector, and Local at 1918x1041 and 1280x800: Galaxy renders 8 contour-aware sector overlays and 11 SVG route paths; each sector renders exactly 8 system overlays and 10 SVG route paths; selected routes use the `map-route-flow` animation; no horizontal overflow, console warnings, or console errors are present.

The deployed custom-domain check repeated the route-boundary and media-delivery verification against the reseeded Azure SQL state. The public root returned `200`; unauthenticated `/app.html` returned the trusted-playground access form with `401`; the promo video returned `200` with a Cloudflare cache hit; and the direct Azure video URL returned `307` to the matching custom-domain URL. The protected app, login, API counts, and deployed Galaxy render had passed in the preceding authenticated smoke.

The 17 July UI-evidence refresh replaced the maintained 1600×900 Command and Galaxy screenshots, removed their redundant public-media copies, and regenerated the film from those canonical captures. Both 900-frame outputs passed full decode, timing, luminance, and audio-tail verification; the Cloudflare derivative is 11.54 MiB. A Release publish contained 66 files but zero video or `assets`/`media` files. Cloudflare revision `e0a90e57-4996-4f5b-97d1-c4e3d003311c` deployed the refreshed landing shell and edge media; live playback reached ready state 4, the hero used one matching preloaded image URL without console errors, and removed screenshot URLs returned `404`.

The duration-independent promo contract is deployed through Cloudflare revision `a190e52e-979f-4ee5-a976-3c62edad5aaf`. Real unauthenticated GETs return `200`, `video/mp4` and `image/jpeg` for the canonical film and poster, with Cloudflare cache hits, revalidation headers and content-derived ETags. GET and HEAD requests for the former duration-based film path return `308` to the canonical film, while a direct Azure request returns `307` to the same Cloudflare URL without serving media bytes. The landing page uses both canonical paths without query strings; browser playback reached ready state 4 and advanced without media or console errors. The consuming-site dependency is therefore cleared. A future real derivative replacement, rather than a sacrificial media deployment, should retain the URL and record the validator change as longitudinal evidence.

The automated coverage includes:

- simulation, influence, economy, orders, movement, combat, admirals, diplomacy, colonisation, Chronicle, Cycle end, continuity, and determinism;
- development and external identity mapping, authorisation, audited admin authority, dashboard admission, visibility, stable API contracts, the hosted access-code gate, protected Development turn advancement, and Worker scheduling;
- the curated opening contract and its complete move, colonise, battle, event, and Chronicle outcome;
- tick rollback, guarded abandonment, recovery, duplicate-running-tick prevention, focused-working-copy equivalence, and a 2,160-tick retained-history scenario;
- strict versioned state-transfer validation across every persisted collection and retained history, including v4 Game-foundation adaptation and the bounded legacy-runtime conversion path;
- migration discovery/application and mandatory live SQL Server coverage for Game-foundation backfill, row-version round trips, external identity/admin audit persistence and immutability, focused tick loading and writes, recovery attempts, Cycle locks, rankings, successor continuity, admirals, diplomacy, and colonisation;
- the SQL-backed running-API development gameplay journey in `eng/alpha-gameplay-smoke.ps1`;
- explicit API and Worker startup failures when SQL configuration is absent;
- live Azure SQL migration/import/export round-trip evidence, deployed authenticated smoke checks, seven-day retention, and an isolated point-in-time restore.

The SQL integration tests remain opt-in locally through `CYCLES_SQL_INTEGRATION_CONNECTION_STRING`. The repository helper now excludes and reports that category when SQL is absent rather than counting its early returns as passing tests. `-RequireSqlIntegration` selects the tagged SQL suites, writes a TRX result, and fails on missing configuration or zero executed tests; the mandatory SQL Server CI job uses that mode. The earlier singleton-guard prerequisite passed 372 non-SQL tests and 33 live SQL tests against the disposable local SQL Server fixture. See the [SQL Server runbook](../database/sqldockerdeploykit/README.md).

The legacy global active-Cycle selectors still fail closed when more than one Active Cycle exists: the in-memory helper and implicit SQL selector return no Cycle for zero, the Cycle for one, and an actionable ambiguity error for two or more. The executable API/Worker whole-state allowance is empty: gameplay reads and commands use an explicit selected Game, scheduled resolution discovers one explicit due Game/Cycle, and compatibility HTTP routes are pinned to the deterministic legacy Game before invoking the same scoped handlers. The account Games projection can enumerate several memberships without loading gameplay state, and the focused Training route can create one private Twin Reaches Game without using a global selector. No safe normal-CLI multi-Game workflow or standard-Game creation path exists.

## Current Boundaries

The development build is suitable for trusted local or access-restricted hosted play-testing. The account Games home, allow-listed private Training provisioning, four-resolution server-derived tutorial journey, pause/skip/fresh-attempt and recovery semantics, selected-Game DTO API, shared browser antiforgery policy, external identity boundary, audited local admin authority, protected dashboard, explicit state-transfer tooling, mandatory SQL hosts, managed-SQL playground cutover, isolated restore proof, and duplicate-safe explicit due-Cycle resolution are implemented contracts. The Development Game retains its **Close command window and advance** exception; Training advances only when the current lesson's authoritative requirements are queued. Before calling the build an alpha or inviting untrusted online players, it still needs MG-10 accessibility and novice-pilot evidence, remaining Worker health/shutdown/fairness/monitoring work in #132, guided play evidence in #131, and the broader security gate in #133.

Gameplay expansion is decision-gated in the [Product Owner Questions](product-owner-questions.md). GitHub issues own actionable work; the [Backlog](backlog.md) curates sequence, decision gates, conditional risks, and links.
